<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Listings</title>
</head>
<body>
    <h3>Edit Your Listings</h3><br>
    <form method="POST" action="/listings/<%= listing._id %>?_method=PUT">
        <input type="text" value="<%= listing.title %>" name="title"><br>
        <textarea name="description" id="" cols="30" rows="10"><%= listing.description %></textarea><br>
        <input type="text" value="<%= listing.image %>" name="image"><br>
        <input type="number" value="<%= listing.price %>" name="price"><br>
        <input type="text" value="<%= listing.location %>" name="location"><br>
        <input type="text" value="<%= listing.country %>" name="country"><br><br>
        <button>Update</button>
    </form>
</body>
</html>